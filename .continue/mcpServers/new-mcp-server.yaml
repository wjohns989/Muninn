## Template: replace values with your MCP server package/configuration
## This file is a template for local development. Replace the placeholder
## package name below with your actual MCP server package before use.
name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: New MCP server
    command: npx
    args:
      - -y
      - your-mcp-server-package-name
    env: {}
