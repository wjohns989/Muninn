# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs (all)
*.log

# Debug / temporary / verification scripts
debug_*.py
debug_*.log
inspect_*.py
inspect_*.txt
verify_*.py
manual_inspect.txt
test_memory.json
test_kuzu_db
test_kuzu_db/
download_models.py

# Runtime logs (explicit)
mcp_wrapper.log
muninn_server.log
consolidation.log
server_debug.log
monitor.log

# Models (large binary files — download separately)
models/
*.gguf
.ollama/
ollama-local-cache/
eval/reports/ollama/
eval/reports/hygiene/
eval/reports/model_benchmarks/
eval/reports/mcp_transport/
eval/reports/memory/

# Loki / session state
.loki/

# Pytest
.pytest_cache/

# OS files
Thumbs.db
.DS_Store

# Data directory (user-specific, stored in ~/.muninn/)
data/
# Phase 17+: curated benchmark datasets in eval/data/ ARE committed
!eval/data/
!eval/data/*.jsonl

# Benchmark and test runtime database artifacts (never commit these)
temp_data/
temp_data_*/
temp_benchmark*/
temp_kuzu*/
test_kuzu_db*/

# Temporary kill/service scripts
kill_server.ps1
service_wrapper.py

# Internal design docs (not for public repo)
DESIGN_BRIEF.md

# Claude / agent session artifacts
.claude/
claudedocs/

# User-specific scripts (hardcoded paths)
run_muninn.bat

# Auth token (contains secrets — never commit)
.muninn_token

# Local environment and credential files (public templates stay trackable)
.env
.env.*
!.env.example
!.env.template
!*.env.example
!*.env.template

# Machine-local agent/session state and personal memories
.serena/
.muninn/

# Runtime process state, traces, profiles, and dumps
*.pid
traces/
*mcp*trace*.jsonl
*request*trace*.jsonl
*tool-call*trace*.jsonl
*tool_call*trace*.jsonl
*.prof
*.pstats
*.heapsnapshot
*.dmp
*.dump

# Runtime databases, indexes, and backend storage mappings
*.db
*.db-*
*.sqlite
*.sqlite3
qdrant*/
kuzu*/
bm25*/
.cache/

# Agent skill configs (user-specific)
skills/

# Large asset binaries (download separately or use Git LFS)
assets/*.png
assets/*.jpg
assets/*.ico
!assets/README.md

.worktrees/
.benchmark_tmp/

# Muninn local runtime (NEVER COMMIT)
.muninn_runtime/
node_modules/
local_docs_backup/
